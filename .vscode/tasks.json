{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Crear tablas SQLAlchemy",
			"type": "shell",
			"command": "python",
			"args": [
				"-c",
				"from app import app, db; app.app_context().push(); db.create_all()"
			]
		},
		{
			"label": "Crear tablas SQLAlchemy",
			"type": "shell",
			"command": "python",
			"args": [
				"-c",
				"from app import app, db; app.app_context().push(); db.create_all()"
			]
		},
		{
			"label": "Crear tablas SQLAlchemy",
			"type": "shell",
			"command": "python",
			"args": [
				"-c",
				"from app import app, db; app.app_context().push(); db.create_all()"
			]
		}
	]
}